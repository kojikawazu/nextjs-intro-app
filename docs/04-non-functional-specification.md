# 非機能仕様書

## プロジェクト情報

| 項目 | 内容 |
|------|------|
| プロジェクト名 | TechProfile Pro |
| ドキュメント種別 | 非機能仕様書 |
| バージョン | 1.0.0 |
| 最終更新日 | 2026-03-20 |

---

## 1. パフォーマンス要件

### 1.1 目標指標

| 指標 | 目標値 | 備考 |
|------|--------|------|
| 初回ページロード時間 | 3秒以内 | LTE回線環境を想定 |
| API レスポンス時間 | 500ms以内 | `/api/portfolio` エンドポイント |
| Time to Interactive (TTI) | 3.5秒以内 | モバイルデバイス基準 |
| Largest Contentful Paint (LCP) | 2.5秒以内 | Core Web Vitals基準 |
| First Input Delay (FID) | 100ms以内 | Core Web Vitals基準 |
| Cumulative Layout Shift (CLS) | 0.1以下 | Core Web Vitals基準 |

### 1.2 現在の実装

#### クライアントサイドレンダリング

- メインページ (`src/app/page.tsx`) は `'use client'` ディレクティブによるクライアントサイドレンダリング (CSR) で動作
- 初回ロード時に `/api/portfolio` エンドポイントからデータを fetch し、ローディングスピナーを表示
- データ取得完了後にコンテンツを描画するため、初回表示までに API レスポンス待ちが発生する

#### APIキャッシュ戦略

```
Cache-Control: public, s-maxage=300, stale-while-revalidate=86400
```

| パラメータ | 値 | 説明 |
|------------|------|------|
| `s-maxage` | 300秒 (5分) | CDNエッジキャッシュの有効期間 |
| `stale-while-revalidate` | 86400秒 (24時間) | キャッシュ期限後もstaleレスポンスを返しつつバックグラウンドで再検証する期間 |
| `public` | - | CDNおよび共有キャッシュでキャッシュ可能 |

この設定により、CDN やリバースプロキシ上でポートフォリオデータのレスポンスがキャッシュされ、5分間は同一レスポンスが即座に返却される。5分経過後も24時間以内であればstaleレスポンスを返しつつ裏で再取得が行われるため、ユーザー体感としてはほぼ常にキャッシュヒットとなる。（Cloud Run 環境では CDN を前段に配置することで効果を発揮する）

#### 画像最適化

- Next.js `Image` コンポーネントを使用 (`next/image`)
- ヒーロー画像には `priority` 属性を付与し、プリロードを実施
- `next.config.js` にて `images.unoptimized: true` が設定されており、Next.js組み込みの画像最適化 (WebP変換、リサイズ等) は無効化されている
- 画像の最適化は外部ソース (GCS上の画像) に依存

#### フォント読み込み

- Google Fonts (`Inter`, `Noto Sans JP`) を CSS `@import` で読み込み
- `@import` による読み込みはレンダリングブロッキングが発生する可能性がある
- `display=swap` パラメータにより、フォント読み込み完了までシステムフォントでフォールバック表示

#### バンドル最適化

- webpack設定により、Node.jsモジュール (`fs`, `net`, `tls`, `crypto`, `stream`, `url`, `zlib`, `http`, `https`, `assert`, `os`, `path`, `child_process`) をクライアントサイドバンドルから除外
- サーバーサイド専用ライブラリ (`@google-cloud/storage`, `resend`) がクライアントバンドルに含まれることを防止

### 1.3 パフォーマンス改善の推奨事項

| 項目 | 現状 | 推奨 |
|------|------|------|
| 画像最適化 | `unoptimized: true` | Next.js Image Optimization の有効化（Cloud Run 環境では外部画像最適化サービスの利用を検討） |
| フォント読み込み | CSS `@import` | `next/font/google` の利用によるセルフホスティングとプリロード |
| レンダリング方式 | CSR | SSR/SSG の検討によるFCP改善 |
| CSSアニメーション | 多数のアニメーション定義 | `prefers-reduced-motion` メディアクエリへの対応 |

---

## 2. スケーラビリティ

### 2.1 アーキテクチャ概要

本プロジェクトはシングルページアプリケーション (SPA) として構築されており、Google Cloud Run 上に Docker コンテナとしてデプロイされる。

| レイヤー | テクノロジー | スケーリング方式 |
|----------|-------------|----------------|
| フロントエンド + API | Next.js 14 (App Router) on Cloud Run | Cloud Run のオートスケーリング（リクエスト数に応じたインスタンス増減） |
| データストレージ | Google Cloud Storage (GCS) | GCSマネージドサービスによる自動スケーリング |
| メール送信 | Resend API | Resend SaaS による自動スケーリング |

### 2.2 スケーラビリティ特性

- **静的資産**: Cloud Run コンテナから直接配信（CDN を前段に配置することでグローバル配信が可能）
- **APIリクエスト**: Serverless Functionsとして実行されるため、同時リクエスト数に応じて自動的にインスタンスが増減する
- **データ取得**: GCSからのデータ取得はAPIキャッシュ (`s-maxage=300`) により、GCSへの直接アクセス頻度を大幅に抑制している
- **コンタクトフォーム**: Resend APIを介したメール送信はステートレスな処理であり、スケーリングの制約とならない

### 2.3 想定トラフィック

| 指標 | 想定値 | 備考 |
|------|--------|------|
| 月間ページビュー | 10,000PV以下 | 個人ポートフォリオサイトとしての想定 |
| 同時接続数 | 50以下 | 通常時の想定 |
| コンタクトフォーム送信 | 月100件以下 | メール送信API呼び出し |

---

## 3. 可用性・信頼性

### 3.1 可用性目標

| 指標 | 目標値 | 備考 |
|------|--------|------|
| 稼働率 | 99.9% | Cloud Run プラットフォームの SLA 準拠 |
| 計画停止 | なし | Cloud Run のローリングアップデートによりゼロダウンタイムデプロイが可能 |

### 3.2 障害対応

#### データ取得の耐障害性

- 開発環境ではGCS障害時にローカルの `sample.json` にフォールバックするロジックが存在する (`src/lib/data-server.ts`)。ただし `sample.json` はリポジトリに含まれていないため、手動で配置しない限りフォールバックは成立しない
- 本番環境ではGCS障害時に HTTP 500 エラーレスポンスを返却
- CDNキャッシュ (`stale-while-revalidate=86400`) により、GCS障害発生後も最大24時間はstaleデータを返却可能

#### メール送信の耐障害性

- Resend API障害時はユーザーにエラーメッセージを表示
- エラーメッセージは内部情報を漏洩しないよう汎用的なメッセージを返却
- 開発環境のみ `console.error` で詳細ログを出力

#### クライアントサイドのエラーハンドリング

- ポートフォリオデータの取得失敗時はエラー画面とリロードボタンを表示
- コンタクトフォーム送信失敗時はエラーメッセージをフォーム内に表示し、再送信を促す

### 3.3 デプロイ戦略

- Cloud Run のローリングアップデートによるゼロダウンタイムデプロイ
- GitHub リポジトリの main ブランチへのプッシュにより GitHub Actions 経由で自動デプロイ（`.github/workflows/deploy_to_googlecloud.yml`）
- Docker イメージを Artifact Registry にプッシュ後、Cloud Run にデプロイ

---

## 4. SEO仕様

### 4.1 メタデータ設定

`src/app/layout.tsx` にて Next.js Metadata API を使用して以下を設定。

#### 基本メタデータ

| 項目 | 設定値 |
|------|--------|
| title | `TechProfile Pro - フリーランスエンジニア` |
| description | `フリーランスエンジニアのポートフォリオサイト` |
| keywords | `フリーランスエンジニア`, `Java`, `TypeScript`, `Next.js`, `バックエンド開発`, `システム開発` |
| authors | `フリーランスエンジニア` |
| creator | `フリーランスエンジニア` |

#### Open Graph (OGP)

| 項目 | 設定値 |
|------|--------|
| type | `website` |
| locale | `ja_JP` |
| url | `https://your-domain.com` |
| title | `TechProfile Pro - フリーランスエンジニア` |
| description | `フリーランスエンジニアのポートフォリオサイト` |
| siteName | `TechProfile Pro` |

#### Twitter Card

| 項目 | 設定値 |
|------|--------|
| card | `summary_large_image` |
| title | `TechProfile Pro - フリーランスエンジニア` |
| description | `フリーランスエンジニアのポートフォリオサイト` |

#### ロボット設定

| 項目 | 設定値 |
|------|--------|
| index | `true` |
| follow | `true` |
| googleBot.index | `true` |
| googleBot.follow | `true` |
| googleBot.max-video-preview | `-1` (無制限) |
| googleBot.max-image-preview | `large` |
| googleBot.max-snippet | `-1` (無制限) |

### 4.2 セマンティックHTML

以下のHTML5セマンティック要素を使用し、検索エンジンによるコンテンツ構造の理解を促進。

| 要素 | 使用箇所 |
|------|----------|
| `<header>` | ページヘッダー (ナビゲーション) |
| `<nav>` | デスクトップ・モバイルナビゲーション |
| `<section>` | Hero, About, Career, Skills, Contact 各セクション |
| `<footer>` | ページフッター (コピーライト) |
| `<h1>` | ヘッダーロゴ、ヒーローテキスト |
| `<h2>` | 各セクションタイトル |

### 4.3 言語設定

- `<html lang="ja">` により日本語コンテンツであることを明示
- 検索エンジンの言語判定およびスクリーンリーダーの読み上げ言語選択に影響

### 4.4 SEO改善の推奨事項

| 項目 | 推奨内容 |
|------|----------|
| OGP画像 | `og:image` の設定追加 |
| canonical URL | `NEXT_PUBLIC_SITE_URL` に合わせたcanonical URLの設定 |
| 構造化データ | JSON-LD による `Person` スキーマの追加 |
| sitemap | `sitemap.xml` の自動生成 |
| robots.txt | `robots.txt` の設置 |

---

## 5. アクセシビリティ仕様

### 5.1 準拠基準

WCAG 2.1 Level AA を目標基準として実装する。

### 5.2 現在の実装

#### ARIA属性

| コンポーネント | ARIA属性 | 設定値 |
|----------------|----------|--------|
| モバイルメニューボタン (`Header`) | `aria-label` | `メニューを開く` |
| SNSリンク (`SocialLinks`) | `aria-label` | `{SNS名}のプロフィールを開く` |

#### キーボードアクセシビリティ

- **フォーカスインジケーター**: `Button` コンポーネントに `focus-visible:ring-2 focus-visible:ring-primary-500 focus-visible:ring-offset-2` を適用
- **フォーム要素**: `Input`, `TextArea` コンポーネントに `focus:ring-2 focus:ring-primary-400` を適用
- **ナビゲーションボタン**: `<button>` 要素を使用しており、キーボード操作 (Tab/Enter/Space) に対応
- **スムーススクロール**: CSS `scroll-behavior: smooth` により、ページ内リンクのスクロールがスムーズに動作

#### フォームアクセシビリティ

- **必須フィールド表示**: 必須項目に赤いアスタリスク (`*`) を表示 (`<span className="ml-1 text-red-400">*</span>`)
- **エラーメッセージ**: フォーム要素直下に赤色テキストでバリデーションエラーを表示
- **ローディング状態**: 送信中はボタンが `disabled` となり、スピナーアニメーションを表示
- **`<label>` 要素**: 各フォーム入力に対応するラベルを配置

#### 色コントラスト

- ダーク背景 (`#0f172a` 〜 `#1e293b`) に対して白色系テキストを使用
- エラー状態は `text-red-400` / `border-red-400` により視覚的に区別

### 5.3 アクセシビリティ改善の推奨事項

| 項目 | 推奨内容 |
|------|----------|
| スキップリンク | メインコンテンツへのスキップナビゲーションの追加 |
| alt属性 | スキルカードアイコンのalt属性の充実化 |
| aria-expanded | モバイルメニューボタンへの `aria-expanded` 属性の追加 |
| aria-current | アクティブなナビゲーション項目への `aria-current` 属性の追加 |
| ライブリージョン | フォーム送信結果の通知に `aria-live` 領域の追加 |
| prefers-reduced-motion | アニメーション無効化の対応 |
| 色だけに依存しない情報伝達 | エラー状態をアイコン等でも示す |

---

## 6. レスポンシブデザイン仕様

### 6.1 ブレークポイント定義

Tailwind CSS のデフォルトブレークポイントを採用。

| デバイス分類 | ブレークポイント | Tailwindプレフィックス |
|------------|--------------|---------------------|
| モバイル | < 640px | (デフォルト) |
| スモールタブレット | >= 640px | `sm:` |
| タブレット | >= 768px | `md:` |
| デスクトップ | >= 1024px | `lg:` |
| ワイドデスクトップ | >= 1280px | `xl:` |

### 6.2 レイアウト対応

#### ナビゲーション

| 画面サイズ | レイアウト |
|-----------|----------|
| モバイル (< 768px) | ハンバーガーメニュー (トグル展開) |
| デスクトップ (>= 768px) | 水平ナビゲーションバー |

#### Aboutセクション

| 画面サイズ | レイアウト |
|-----------|----------|
| モバイル | 1カラム (縦並び、中央揃え) |
| デスクトップ (>= 1024px) | 2カラムグリッド (プロフィール画像 + テキスト) |

#### Skillsセクション

| 画面サイズ | グリッドカラム数 |
|-----------|---------------|
| モバイル | 1カラム |
| タブレット (>= 768px) | 2カラム |
| デスクトップ (>= 1024px) | 3カラム |
| ワイドデスクトップ (>= 1280px) | 4カラム |

#### Careerセクション

| 画面サイズ | レイアウト |
|-----------|----------|
| モバイル | カードのみ (タイムラインの縦線・ドットは非表示) |
| デスクトップ (>= 768px) | 左側にタイムライン縦線・ドット + 右側にカード |

#### コンテナ

- 最大幅: `max-w-7xl` (1280px)
- 左右パディング: `px-4` (モバイル) / `px-6` (sm) / `px-8` (lg)
- 水平中央揃え: `mx-auto`

#### セクション間の余白

- 標準: `py-20` (モバイル) / `py-32` (lg) - `section-padding` ユーティリティクラスとして定義

### 6.3 タイポグラフィのレスポンシブ対応

| 要素 | モバイル | デスクトップ |
|------|---------|-----------|
| ヒーロータイトル (`h1`) | `text-4xl` | `text-6xl` (lg) |
| セクションタイトル (`h2`) | `text-3xl` | `text-4xl` (lg) |
| ヘッダーロゴ | `text-xl` | `text-2xl` (lg) |
| ヘッダー高さ | `h-16` | `h-20` (lg) |

---

## 7. ブラウザ互換性

### 7.1 対応ブラウザ

| ブラウザ | 最低バージョン | 備考 |
|----------|-------------|------|
| Google Chrome | 最新2バージョン | 推奨ブラウザ |
| Mozilla Firefox | 最新2バージョン | |
| Safari | 最新2バージョン | macOS / iOS |
| Microsoft Edge | 最新2バージョン | Chromiumベース |

### 7.2 使用CSS機能の互換性

| CSS機能 | 使用箇所 | 互換性リスク |
|---------|---------|------------|
| `backdrop-filter: blur()` | `glass-effect`, `glass-card` | Safari では `-webkit-backdrop-filter` が必要 (Tailwind CSSが自動プレフィックス付与) |
| CSS Grid | スキルカード、Aboutレイアウト | 主要ブラウザで対応済み |
| `scroll-behavior: smooth` | ページ内ナビゲーション | 主要ブラウザで対応済み |
| `mask-composite: exclude` | `gradient-border` | ブラウザ間で挙動差あり |
| `focus-visible` | Button, Input | 主要ブラウザで対応済み |
| CSS Custom Properties | Tailwind CSS内部 | 主要ブラウザで対応済み |
| CSS Animations / Keyframes | 各種アニメーション | 主要ブラウザで対応済み |

### 7.3 JavaScript互換性

- **ターゲット**: ES2017+ (async/await, Optional chaining等)
- **React 18.3.1**: 主要モダンブラウザで対応
- **Next.js 14.2.5**: 自動ポリフィル・トランスパイルにより互換性を確保

---

## 8. 監視・ログ

### 8.1 現在のログ出力

| ログ種別 | 出力条件 | 出力先 |
|----------|---------|--------|
| API データ取得ログ | 常時 | サーバーサイドコンソール |
| GCS アクセスログ | 常時 | サーバーサイドコンソール |
| メール送信エラー | 開発環境のみ (`NODE_ENV === 'development'`) | サーバーサイドコンソール |
| コンタクトフォームエラー | 開発環境のみ | サーバーサイドコンソール / ブラウザコンソール |

### 8.2 監視の推奨事項

| 項目 | 推奨内容 |
|------|----------|
| エラー監視 | Sentry等のエラートラッキングサービスの導入 |
| パフォーマンス監視 | Cloud Monitoring または Web Vitals の計測導入 |
| 稼働監視 | UptimeRobot等によるヘルスチェック監視 |
| ログ管理 | 構造化ログの導入と本番環境でのログレベル制御 |

---

## 9. テスト

### 9.1 現在のテスト環境

| 項目 | 状況 |
|------|------|
| ユニットテスト | 未導入 |
| 統合テスト | 未導入 |
| E2Eテスト | 未導入 |
| リンティング | ESLint (`eslint-config-next`) を使用 |
| 型チェック | TypeScript (`tsc --noEmit`) を使用 |
| コードフォーマット | Prettier を使用 |

### 9.2 テスト導入の推奨事項

| テスト種別 | 推奨ツール | 対象 |
|-----------|----------|------|
| ユニットテスト | Jest + React Testing Library | コンポーネント、バリデーション関数 |
| APIテスト | Jest + supertest | API Routes |
| E2Eテスト | Playwright | ページ遷移、フォーム送信 |
| アクセシビリティテスト | axe-core + jest-axe | WCAG準拠チェック |
| パフォーマンステスト | Lighthouse CI | Core Web Vitals |
